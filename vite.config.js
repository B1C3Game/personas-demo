import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  base: '/personas-demo/', // Set this to your repo name if deploying to GitHub Pages
})
